type key = string list

type token = (key * int)

type media = {
  ?id : string option;
  filename : string;
  size : int;
  metadata : (string * string) list <json repr="object">;
}

type medias = media list
